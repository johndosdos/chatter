version: "2"

run:
  timeout: 5m

linters:
  default: standard
  enable:
    # Core Bug Finders
    - govet
    - errcheck
    - staticcheck
    - unused
    - ineffassign
    - bodyclose
    
    # Security & Readability
    - gosec
    - goconst

output:
  sort-order:
    - file
    - linter
    - severity